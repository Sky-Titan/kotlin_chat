<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".Activity.SignUpActivity">

    <EditText
            android:id="@+id/userID_edit_signup"
            android:hint="ID"
            android:theme="@style/EditTextStyle"
            android:layout_gravity="center_vertical"
            android:layout_marginTop="150dp"
            android:layout_marginHorizontal="@dimen/signin_marginHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
    <EditText
            android:id="@+id/userPW_edit_signup"
            android:inputType="textPassword"
            android:hint="password"
            android:theme="@style/EditTextStyle"
            android:layout_gravity="center_vertical"
            android:layout_marginHorizontal="@dimen/signin_marginHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
    <EditText
            android:id="@+id/userPW_Confirm_edit_signup"
            android:inputType="textPassword"
            android:hint="password"
            android:theme="@style/EditTextStyle"
            android:layout_gravity="center_vertical"
            android:layout_marginHorizontal="@dimen/signin_marginHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
    <Button
            android:id="@+id/signup_btn_signup"
            android:text="회원가입 완료"
            android:textColor="@color/white"
            android:background="@drawable/ripple_lime_green"
            android:layout_marginTop="@dimen/signin_marginTop"
            android:layout_marginHorizontal="@dimen/signin_marginHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
</LinearLayout>